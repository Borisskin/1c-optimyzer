---
id: virtual_table_in_join
severity: warning
category: performance
patterns:
  - '(?im)СОЕДИНЕНИЕ\s+\S+(?:\.\S+)*\.(Остатки|Обороты|ОстаткиИОбороты|СрезПоследних|СрезПервых|ОстаткиИОбороты)\s*\('
  - '(?im)JOIN\s+\S+(?:\.\S+)*\.(Balance|Turnovers|BalanceAndTurnovers|SliceLast|SliceFirst)\s*\('
tags: [tsup-2.13.4, expert-10]
---

# Виртуальная таблица регистра в соединении

## Что не так

Виртуальные таблицы 1С (`Остатки`, `Обороты`, `ОстаткиИОбороты`, `СрезПоследних`, `СрезПервых`) **не материализованы** в базе данных — это набор запросов, которые платформа разворачивает в момент исполнения. Использование виртуальной таблицы в `СОЕДИНЕНИЕ` заставляет СУБД повторно вычислять её при каждом сравнении строк левой таблицы.

## Почему это плохо

Виртуальная таблица может содержать сложные подзапросы с группировками по периодам, расчётами остатков, агрегациями. `СОЕДИНЕНИЕ` фактически выполнит этот сложный подзапрос O(N×M) раз вместо одного.

## Как переписать

Вынести виртуальную таблицу в **отдельный шаг** через временную таблицу с индексом:

```sdbl
ВЫБРАТЬ
    Остатки.Номенклатура,
    Остатки.Склад,
    Остатки.КоличествоОстаток
ПОМЕСТИТЬ ВТ_Остатки
ИЗ
    РегистрНакопления.ТоварыНаСкладах.Остатки(&Дата) КАК Остатки
ИНДЕКСИРОВАТЬ ПО
    Номенклатура, Склад
;

ВЫБРАТЬ ...
ИЗ
    Документ.РеализацияТоваровУслуг.Товары КАК Реализация
    ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Остатки КАК Остатки
        ПО Реализация.Номенклатура = Остатки.Номенклатура
            И Реализация.Склад = Остатки.Склад
```

## Связано в методике

- ЦУП 2.13.4 — «Виртуальные таблицы и временные таблицы»
- Раздел 10 курса 1С:Эксперт — «Запросы которые работают быстро», тема «Соединение с виртуальной таблицей»
