# Sprint 1+ — Real-data acceptance gate configuration.
#
# Copy this file to `.env.test` (which is gitignored) and set
# OPTIMYZER_REAL_FOLDER_PATH to an actual local folder with TJ logs.
#
# Tests in `backend/tests/test_sprint1_real_folder.py` are auto-skipped
# when the env var is missing or the folder does not exist.
OPTIMYZER_REAL_FOLDER_PATH=D:\1C-Optimyzer\1c-optimyzer\logs
